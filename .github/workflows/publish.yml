name: Docker

on:
  schedule:
    - cron: '0 3 * * 0,3,6'
  workflow_dispatch:
  push:
    tags: [ 'v*' ]

env:
  IMAGE_NAME: ${{ github.repository }}
  REGISTRY: ${{ vars.REGISTRY }}
  SCHEME: ${{ vars.SCHEME }}
  REPO: ${{ vars.REPO }}
  DOCFILES: ${{ vars.DOCFILES }}
  SRCFILES: ${{ vars.SRCFILES }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      # Checkout the repository
      - name: Checkout  🎯
        uses: actions/checkout@v3
        with:
          fetch-depth: 1

      # Restore cached repository
      # https://github.com/actions/cache/tree/main/restore
      - name: Restore cached repository 📦
        uses: actions/cache/restore@v3
        with:
          path: ${{ github.workspace }}/texlive/
          key: rsync-${{ env.REPO }}-${{ github.sha }}
          restore-keys: |
            rsync-${{ env.REPO }}-
            rsync-
          fail-on-cache-miss: false

      - name: rsync 📁
        shell: bash
        run: ${{ github.workspace }}/script/rsync.sh ${{ env.REPO }} ${{ github.workspace }}/texlive/ --exclude-from=${{ github.workspace }}/rsyncignore.txt

      # Save repository to cache
      # https://github.com/actions/cache/tree/main/save
      - name: Save repository to cache 💾
        uses: actions/cache/save@v3
        with:
          path: ${{ github.workspace }}/texlive/
          key: rsync-${{ env.REPO }}-${{ github.sha }}

      # Add support for more platforms with QEMU
      # https://github.com/docker/setup-qemu-action
      - name: Set up QEMU 💻
        uses: docker/setup-qemu-action@v2

      # Add support for Buildx
      # https://github.com/docker/setup-buildx-action
      - name: Setup buildx 🔧
        uses: docker/setup-buildx-action@v2

      # Extract metadata (tags, labels)
      # https://github.com/docker/metadata-action
      - name: Extract metadata 🔍
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          bake-target: docker-metadata-action
          flavor: |
            latest=auto
            prefix=bullseye-full-,onlatest=false
          tags: |
            type=schedule,pattern=nightly
            type=semver,pattern=v{{version}}
            type=semver,pattern=v{{major}}.{{minor}}
            type=semver,pattern=v{{major}},enable=${{ !startsWith(github.ref, 'refs/tags/v0.') }}
            type=ref,event=pr,pattern=pr{{number}}
            type=sha,prefix=,format=short
          labels: |
            org.opencontainers.image.source=${{ github.event.repository.clone_url }}

      # Merge metadata (labels, tags) with build args
      # Workaround: https://github.com/docker/metadata-action/issues/124#issuecomment-1018441005
      - name: Merge metadata 🚚
        id: merge
        run: jq -s '.[0]' ${{ steps.meta.outputs.bake-file }} > docker-bake.override.json

      # Login against a Docker registry except on PR
      # https://github.com/docker/login-action
      - name: Log into ${{ env.REGISTRY }} 📨
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Build Docker image with Bake
      # https://github.com/docker/bake-action
      - name: Build only 💡
        uses: docker/bake-action@v2
        if: ${{ github.event_name == 'pull_request' }}
        with:
          load: true
          files: docker-bake.hcl,docker-bake.override.json
          set: |
            build.cache-from=type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache

      - name: Build and push Docker image 🐳
        if: ${{ github.event_name != 'pull_request' }}
        uses: docker/bake-action@v2
        with:
          push: true
          files: docker-bake.hcl,docker-bake.override.json
          set: |
            *.cache-to=type=inline
            build.cache-from=type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
            build.cache-to=type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache,mode=max
